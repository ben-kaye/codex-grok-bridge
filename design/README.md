# Design

- `invariants/`: constraints every implementation must preserve.
- `feats/`: user-visible behavior and acceptance criteria.
- `plans/`: ordered implementation and verification work.

Read invariants, then the relevant feature, then its active plan.

